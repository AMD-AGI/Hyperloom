# TRITON Constraints (auto-extracted)

*Generated by bootstrap on 2026-04-16T23:49:53.184303*
*Source: 479 files scanned*

## DO NOT CHANGE (critical)

- **aiter/ops/triton/activation.py:51**: Hardcoded — author says do not change: # This is fixed by spec for MXFP4. Do not tune this.
  ```
  # This is fixed by spec for MXFP4. Do not tune this.
  ```

- **aiter/ops/triton/moe/moe_op_gemm_a4w4.py:132**: Hardcoded — author says do not change: # This is fixed by spec for MXFP4. Do not tune this.
  ```
  # This is fixed by spec for MXFP4. Do not tune this.
  ```

- **aiter/ops/triton/quant/fused_mxfp4_quant.py:590**: Hardcoded — author says do not change: # This is fixed by spec for MXFP4. Do not tune this.
  ```
  # This is fixed by spec for MXFP4. Do not tune this.
  ```

- **aiter/ops/triton/quant/quant.py:154**: Hardcoded — author says do not change: # This is fixed by spec for MXFP4. Do not tune this.
  ```
  # This is fixed by spec for MXFP4. Do not tune this.
  ```

- **aiter/ops/triton/moe/moe_op_gemm_a4w4.py:132**: Hardcoded — author says do not change: # This is fixed by spec for MXFP4. Do not tune this.
  ```
  # This is fixed by spec for MXFP4. Do not tune this.
  ```

- **aiter/ops/triton/quant/fused_mxfp4_quant.py:590**: Hardcoded — author says do not change: # This is fixed by spec for MXFP4. Do not tune this.
  ```
  # This is fixed by spec for MXFP4. Do not tune this.
  ```

- **aiter/ops/triton/quant/quant.py:154**: Hardcoded — author says do not change: # This is fixed by spec for MXFP4. Do not tune this.
  ```
  # This is fixed by spec for MXFP4. Do not tune this.
  ```

## Constraints (warning)

- aiter/ops/triton/activation.py:51: Hardcoded by specification: # This is fixed by spec for MXFP4. Do not tune this.
- aiter/ops/triton/causal_conv1d.py:118: Assert constraint: x.dim() == 2 
- aiter/ops/triton/causal_conv1d.py:120: Assert constraint: query_start_loc.dim() == 1 
- aiter/ops/triton/causal_conv1d.py:124: Assert constraint: bias.dim() == 1 
- aiter/ops/triton/causal_conv1d.py:125: Assert constraint: dim == bias.size(0) 
- aiter/ops/triton/causal_conv1d.py:127: Assert constraint: cache_indices.dim() == 1 
- aiter/ops/triton/causal_conv1d.py:128: Assert constraint: padded_batch == cache_indices.size(0) 
- aiter/ops/triton/causal_conv1d.py:130: Assert constraint: has_initial_state.size() == (padded_batch,) 
- aiter/ops/triton/causal_conv1d.py:135: Assert constraint: (dim, width) == weight.shape 
- aiter/ops/triton/causal_conv1d.py:247: Assert constraint: dim == weight.size(0) 
- aiter/ops/triton/causal_conv1d.py:253: Assert constraint: dim == conv_state.size(1) 
- aiter/ops/triton/causal_conv1d.py:255: Assert constraint: conv_state.size(0) >= batch 
- aiter/ops/triton/gather_kv_b_proj.py:51: Assert constraint: ChunkK % block_size == 0 
- aiter/ops/triton/gmm.py:56: Assert constraint: torch.all(group_sizes >= 0).item() All group_sizes must be non-negative.
- aiter/ops/triton/gmm.py:57: Assert constraint: grid_dim > 0, f"Grid dimension must be positive (it's {grid_dim})." 
- aiter/ops/triton/gmm.py:59: Assert constraint: torch.all(num_m_tiles >= 0).item() All num_m_tiles must be non-negative.
- aiter/ops/triton/gmm.py:61: Assert constraint: num_n_tiles > 0, f"num_n_tiles must be positive, it's {num_n_tiles}." 
- aiter/ops/triton/gmm.py:63: Assert constraint: num_tiles > 0, f"num_tiles must be positive, it's {num_tiles}." 
- aiter/ops/triton/gmm.py:225: Assert constraint: grid_dim > 0, f"Grid dimension must be positive (it's {grid_dim})." 
- aiter/ops/triton/gmm.py:227: Assert constraint: num_k_tiles > 0, f"num_k_tiles must be positive, it's {num_k_tiles}." 
