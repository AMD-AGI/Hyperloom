name: compiler_tuning
family: long
cost_minutes_p50: 45.0
cost_minutes_p75: 75.0
expected_gain_pct: [1.0, 4.0]
accuracy_risk: 0.05
crash_risk: 0.15
prerequisites:
  - profile
requires_lanes:
  - workspace_mutation
  - benchmark_lane
allowed_tools:
  - emit_intent
  - Read
  - Bash
  - Edit
side_effects:
  - patches_workspace
  - rebuilds_extension
  - writes_results
allowed_modes:
  - marathon_multi_agent
preferred_backend: claude
preferred_model: claude-opus-4-7
max_turns: 60
lease_ttl_sec: 4500
applicable_when:
  - "torch_compile_enabled"
  - "marathon_only"
