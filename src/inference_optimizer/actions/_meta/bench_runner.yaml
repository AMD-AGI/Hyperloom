name: bench_runner
family: prep
cost_minutes_p50: 3.0
cost_minutes_p75: 6.0
expected_gain_pct: [0.0, 0.0]   # measurement only — no gain expected
accuracy_risk: 0.0
crash_risk: 0.05
prerequisites: []
requires_lanes:
  - benchmark_lane
allowed_tools:
  - emit_intent
  - Read
  - Bash
side_effects:
  - reads_server
  - writes_results
allowed_modes:
  - quick_param_sweep
  - guided_kernel_opt
  - marathon_multi_agent
preferred_backend: claude
preferred_model: claude-opus-4-7
max_turns: 20
lease_ttl_sec: 900
applicable_when:
  - "any"
