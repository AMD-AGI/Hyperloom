name: setup
family: prep
cost_minutes_p50: 2
cost_minutes_p75: 5
expected_gain_pct: [0, 0]   # no perf gain on its own
accuracy_risk: 0.0
crash_risk: 0.05
prerequisites: []
requires_lanes: [server_lifecycle]
allowed_tools: [Read, Bash, Glob]
side_effects: [server_restart]
allowed_modes: [quick_param_sweep, guided_kernel_opt, marathon_multi_agent]
preferred_backend: claude
preferred_model: claude-opus-4-7
max_turns: 8
lease_ttl_sec: 600
applicable_when: [run_started]
