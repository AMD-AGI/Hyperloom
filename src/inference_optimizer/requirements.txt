# Inference Optimizer — runtime dependencies
#
# pip install -r requirements.txt
#
# Note: the bootstrap module installs Node.js >=18 and the @anthropic-ai/claude-code
# CLI itself; those are NOT pip packages.

# Needed by ClaudeBackend (Phase 6.1). 0.1.65 introduced
# ``create_sdk_mcp_server`` + the ``@tool`` decorator that we use to register
# the in-process ``emit_intent`` MCP tool. Latest on PyPI as of 2026-04 is
# 0.1.69 — we pin >=0.1.65 to keep dev installs unblocked.
claude-agent-sdk>=0.1.65

# Needed by CodexBackend (Phase 6.2). Critic / Sage roles call OpenAI's
# chat.completions API and parse a fenced ``validated_json_output`` envelope
# (DESIGN §10.5.5). 1.50 introduced ``AsyncOpenAI(http_client=...)`` which we
# need for self-signed-cert proxies.
openai>=1.50

# httpx ships with the openai SDK already, but we touch it explicitly when
# the user opts into ``verify_ssl=False`` for proxy deployments.
httpx>=0.27

# Action metadata loader (Phase 4 / F3) — reads actions/_meta/*.yaml.
PyYAML>=6.0

# Test runner — only needed for `pytest`. Listed for convenience.
pytest>=8.0
pytest-asyncio>=1.0
