# Phase E — test classification inventory
#
# Method: cross-referenced the Phase 0 guardrail keep-list, scanned every
# inference_optimizer/tests/test_*.py for outward-observable-signal tokens
# (session_breakdown / to_dict / build() / state.json / final.json / CLI
# parse_args / --flags / exit codes / envelope / validate_envelope /
# stop_reason / to_prompt_summary / manifest / schema), and hand-reviewed
# the low-signal files.
#
# Tags: GUARDRAIL (Phase 0 keep-list, never delete) / BEHAVIOR (covers an
# outward contract) / PRIVATE (asserts a private helper/internal state) /
# STALE (tests a removed feature).
#
# Snapshot: inference_optimizer/tests = 176 files.

## Summary
GUARDRAIL : 8 main-package files (+ 9 agent files in the keep-list) — keep, slim-only.
BEHAVIOR  : ~168 files — overwhelming majority assert outward contracts
            (CLI flags/exit, session_breakdown / state.json / final.json
            artifacts, envelope schema, policy verdicts, phase routing,
            KB store/client behavior, resume round-trips, prompt content).
PRIVATE   : ~0 standalone files. The suite already went through a
            private/duplicate-test cleanup round:
              - a6b39547 Remove duplicate prompt asset tests (test_prompt_assets.py)
              - 94a87099 Remove private prompt formatter tests
                          (test_prompt_renders_gain_values.py)
            and Phases A/B deleted the proxy/silent-tick unit tests inline
            (c8b17d80, 34a0603a). No large pool of PRIVATE-only files remains.
STALE     : ~0. Grepped for every feature removed in A/B/D
            (consecutive_silent_ticks, params_no_promote_streak PROXY,
            last_select_kernels field, cortex_kb_constants, kb-flusher,
            IDLE_CLOSE_TICKS, no-more-leverage run-loop net, intent_parser
            module path). All surviving references are LIVE behavior:
              - params_no_promote_streak: kept as an LLM-visible FACT
                (test_drop_scoreboard) — BEHAVIOR, not stale.
              - skip_to_sweep / no_more_leverage: still the live phase_state
                routing for steward stop_session (test_assess_remaining_gaps,
                test_phase_state_plateau) — BEHAVIOR.
              - last_select_kernels: only a migration drop-list entry + one
                vestigial no-op `= {}` set inside live BEHAVIOR tests; not a
                standalone stale test.

## Guardrail keep-list (main package) — KEEP, slim-only
inference_optimizer/tests/test_coordinator_runtime.py             GUARDRAIL  coordinator full-flow + phase machine
inference_optimizer/tests/test_e2e_mock.py                        GUARDRAIL  mock end-to-end optimize -> CLOSE
inference_optimizer/tests/test_breakdown_smoke.py                 GUARDRAIL  session_breakdown top-level shape
inference_optimizer/tests/test_back_compat_legacy_field_name.py   GUARDRAIL  v1 alias keys + extra_sglang_args alias
inference_optimizer/tests/test_cli_breakdown_claw_mirror.py       GUARDRAIL  claw-stats mirror consumer
inference_optimizer/tests/test_no_legacy_writer_sites.py          GUARDRAIL  static legacy-field allowlist guard
inference_optimizer/tests/test_payload_aliases.py                 GUARDRAIL  payload alias helper contract
inference_optimizer/tests/test_roofline_ceiling.py                GUARDRAIL  roofline ceiling outward report numbers

## Safe consolidation candidates (same module, no coverage loss)
inference_optimizer/tests/test_apply_kernel_patch_roots.py    BEHAVIOR  -> fold into test_framework_paths_units.py (both test orchestrator.framework_paths)
inference_optimizer/tests/test_framework_source_roots.py      BEHAVIOR  -> fold into test_framework_paths_units.py (framework_paths source-root resolution)

## Decision
Per the Phase E conduct ("删的只能是测私有实现/已失效的"; "禁止删后降低对外行为覆盖"),
no behavior/contract tests are deleted. STALE/PRIVATE residue is ~0 (already
cleaned). Phase E action is limited to merging the cohesive same-module
framework_paths test trio; the rest of the suite is already behavior-focused
and single-purpose, so further merging would be churn ("搬家"), not a
complexity reduction.
