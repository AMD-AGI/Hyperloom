usage: inference_optimizer [-h] [--verbose] {optimize} ...

Inference Optimizer v0.6 — multi-agent SGLang/vLLM optimization

positional arguments:
  {optimize}
    optimize     Drive a multi-agent optimization run on a model

options:
  -h, --help     show this help message and exit
  --verbose, -v  Verbose logging (-v INFO, -vv DEBUG)

==================== optimize --help ====================
usage: inference_optimizer optimize [-h] [--model MODEL]
                                    [--gpu-type {mi300x,mi325x,mi355x}]
                                    [--framework {sglang,vllm,atom}]
                                    [--nodes NODES]
                                    [--rayjob-image RAYJOB_IMAGE]
                                    [--rayjob-gpus-per-node RAYJOB_GPUS_PER_NODE]
                                    [--rayjob-extra-env K=V] [--tp TP]
                                    [--conc CONC] [--ep EP]
                                    [--pd-mode {colocated,disaggregated}]
                                    [--pd-prefill-nodes PD_PREFILL_NODES]
                                    [--pd-decode-nodes PD_DECODE_NODES]
                                    [--pd-prefill-tp PD_PREFILL_TP]
                                    [--pd-decode-tp PD_DECODE_TP]
                                    [--pd-transfer-backend PD_TRANSFER_BACKEND]
                                    [--pd-ib-device PD_IB_DEVICE]
                                    [--skip-variants SKIP_VARIANTS]
                                    [--max-hours MAX_HOURS]
                                    [--closing-grace-sec CLOSING_GRACE_SEC]
                                    [--isl ISL] [--osl OSL]
                                    [--precision PRECISION]
                                    [--framework-version FRAMEWORK_VERSION]
                                    [--continue-kernel-after-gemm | --no-continue-kernel-after-gemm]
                                    [--target-gain TARGET_GAIN | --target-tput TARGET_TPUT | --target-baseline-dir TARGET_BASELINE_DIR]
                                    [--resume] [--resume-from RESUME_FROM]
                                    [--force-resume]
                                    [--model-class MODEL_CLASS]
                                    [--target-summary TARGET_SUMMARY]
                                    [--compare-against-gpu COMPARE_AGAINST_GPU]
                                    [--max-ticks MAX_TICKS]
                                    [--tick-interval-sec TICK_INTERVAL_SEC]
                                    [--claude-model CLAUDE_MODEL]
                                    [--codex-model CODEX_MODEL] [--no-kernel]
                                    [--no-explore]
                                    [--launch-info-file LAUNCH_INFO_FILE]
                                    [--framework-pr-discover-timeout-sec FRAMEWORK_PR_DISCOVER_TIMEOUT_SEC]
                                    [--no-framework] [--kernel-codex]
                                    [--kernel-claude] [--critic-mock]
                                    [--critic-agent] [--robustness-mock]
                                    [--robustness-agent]
                                    [--robustness-server-url ROBUSTNESS_SERVER_URL]
                                    [--robustness-llm-rca]
                                    [--no-robustness-llm-rca]
                                    [--robustness-workload-uid ROBUSTNESS_WORKLOAD_UID]
                                    [--robustness-disable-local-probe]
                                    [--no-robustness-disable-local-probe]
                                    [--robustness-enable-cluster-pod-metrics]
                                    [--no-robustness-enable-cluster-pod-metrics]
                                    [--robustness-pod-metrics-categories ROBUSTNESS_POD_METRICS_CATEGORIES]
                                    [--orch-prompt ORCH_PROMPT]
                                    [--critic-prompt CRITIC_PROMPT]
                                    [--kernel-prompt KERNEL_PROMPT]
                                    [--cortex-kb-url CORTEX_KB_URL]
                                    [--local-kb-root LOCAL_KB_ROOT]
                                    [--degraded-kb]
                                    [--cortex-strict-fingerprint]
                                    [--no-warm-replay]
                                    [--warm-replay-min-confidence WARM_REPLAY_MIN_CONFIDENCE]
                                    [--warm-replay-min-reproduce-pct WARM_REPLAY_MIN_REPRODUCE_PCT]
                                    [--no-kb-flusher]
                                    [--kb-flusher-interval-sec KB_FLUSHER_INTERVAL_SEC]
                                    [--kb-flusher-batch-size KB_FLUSHER_BATCH_SIZE]
                                    [--pr-monitor-url PR_MONITOR_URL]
                                    [--pr-monitor-mcp-url PR_MONITOR_MCP_URL]
                                    [--degraded-pr]
                                    [--pr-feed-window-days PR_FEED_WINDOW_DAYS]
                                    [--research-lane-capacity RESEARCH_LANE_CAPACITY]
                                    [--gpu-specialist-capacity GPU_SPECIALIST_CAPACITY]
                                    [--proposal-scorer-models PROPOSAL_SCORER_MODELS]
                                    [--no-proposal-scoring]
                                    [--specialist-model SPECIALIST_MODEL]
                                    [--dynamic-action-model DYNAMIC_ACTION_MODEL]
                                    [--dynamic-action-turn-cap DYNAMIC_ACTION_TURN_CAP]
                                    [--dynamic-action-wall-clock-sec DYNAMIC_ACTION_WALL_CLOCK_SEC]
                                    [--specialist-max-turns SPECIALIST_MAX_TURNS]
                                    [--specialist-per-turn-max-seconds SPECIALIST_PER_TURN_MAX_SECONDS]
                                    [--specialist-dispatch-mode {subprocess,inprocess}]
                                    [--specialist-mcp-config SPECIALIST_MCP_CONFIG]
                                    [--allow-empty-kernel-shape]
                                    [--enable-roofline | --no-enable-roofline]
                                    [--research-scout | --no-research-scout]
                                    [--research-scout-interval RESEARCH_SCOUT_INTERVAL]
                                    [--recipe-sediment | --no-recipe-sediment]
                                    [--target-advisory | --no-target-advisory]
                                    [--depth-gate | --no-depth-gate]
                                    [--depth-gate-thresholds DEPTH_GATE_THRESHOLDS]
                                    [--enable-conc-sweep | --no-enable-conc-sweep]
                                    [--conc-sweep-concs CONC_SWEEP_CONCS]
                                    [--conc-sweep-timeout-sec CONC_SWEEP_TIMEOUT_SEC]
                                    [--conc-sweep-total-budget-sec CONC_SWEEP_TOTAL_BUDGET_SEC]
                                    [--explore-overtime-kill-ratio EXPLORE_OVERTIME_KILL_RATIO]
                                    [--explore-variant-timeout-sec EXPLORE_VARIANT_TIMEOUT_SEC]
                                    [--explore-variant-timeout-safety-margin EXPLORE_VARIANT_TIMEOUT_SAFETY_MARGIN]
                                    [--explore-roofline-hard-gate]
                                    [--legacy-action-scores {drop,warn}]
                                    [--migration-mode {strict,lenient}]
                                    [--reset-state]
                                    [--breakdown-include-transcripts {true,false}]
                                    [--plateau-explore-keep-gain PLATEAU_EXPLORE_KEEP_GAIN]
                                    [--plateau-explore-empty-streak PLATEAU_EXPLORE_EMPTY_STREAK]
                                    [--plateau-explore-lookback PLATEAU_EXPLORE_LOOKBACK]
                                    [--plateau-kernel-revert-streak PLATEAU_KERNEL_REVERT_STREAK]
                                    [--plateau-kernel-keep-gain PLATEAU_KERNEL_KEEP_GAIN]
                                    [--plateau-kernel-lookback PLATEAU_KERNEL_LOOKBACK]
                                    [--explore-force-exit-hours-remaining EXPLORE_FORCE_EXIT_HOURS_REMAINING]
                                    [--explore-force-exit-budget-pct EXPLORE_FORCE_EXIT_BUDGET_PCT]
                                    [--steward-disabled]
                                    [--steward-continuation-cap STEWARD_CONTINUATION_CAP]
                                    [--max-minutes-prelude-pct PHASE_BUDGET_PRELUDE_PCT]
                                    [--max-minutes-explore-pct PHASE_BUDGET_EXPLORE_PCT]
                                    [--max-minutes-kernel-pct PHASE_BUDGET_KERNEL_PCT]
                                    [--max-minutes-sweep-pct PHASE_BUDGET_SWEEP_PCT]
                                    [--max-minutes-close-pct PHASE_BUDGET_CLOSE_PCT]
                                    [--strict-phase] [--no-strict-phase]

options:
  -h, --help            show this help message and exit
  --model MODEL, -m MODEL
                        Model path (required for new runs; ignored when
                        --resume is set — model is read from
                        manifest.json/state.json)
  --gpu-type {mi300x,mi325x,mi355x}
                        Hint for the real target GPU. The rocm-smi probe
                        always wins when both are present and disagree; a WARN
                        is emitted to stderr so the operator sees the typo.
                        Used verbatim only when the probe fails (CPU sandbox /
                        no rocm-smi). Magpie runner_type is derived
                        separately; mi325x currently runs with mi300x runner
                        scripts because Magpie does not yet ship
                        sglang_mi325x.sh / vllm_mi325x.sh.
  --framework {sglang,vllm,atom}
                        Inference framework to benchmark / optimize.
                        Resolution order: --framework > $FRAMEWORK env >
                        sglang (default). Selection is session-wide; mixing
                        frameworks in a single session is not supported. NOTE:
                        --framework atom is single-node-only (``--nodes>=2``
                        fails fast); profile / roofline, kernel-agent, and
                        framework-agent are all enabled on atom. The auto-
                        tighten guard only enforces ``--nodes 1``.
  --nodes NODES         Total number of GPU nodes for the inference RayJob. 1
                        (default) keeps the legacy single-pod path. >=2:
                        `optimize` provisions the SaFE RayJob before preflight
                        (unless already in /tmp/multi_node_state.json), runs
                        bootstrap once, and exports RAY_ADDRESS for kernel-
                        agent. Does not stop the RayJob on exit; run `python3
                        -m inference_optimizer.multi_node stop-rayjob` when
                        you want to release it. Requires --rayjob-image or
                        INFERENCE_OPTIMIZER_RAYJOB_IMAGE. Resolution: --nodes
                        > $INFERENCE_OPTIMIZER_NODES > $NODES > 1.
  --rayjob-image RAYJOB_IMAGE
                        Container image for the multi-node RayJob
                        (head+workers). Required when --nodes>=2 unless
                        INFERENCE_OPTIMIZER_RAYJOB_IMAGE is set or state file
                        last_create_request.image is present.
  --rayjob-gpus-per-node RAYJOB_GPUS_PER_NODE
                        GPUs per RayJob pod (default:
                        INFERENCE_OPTIMIZER_GPUS_PER_NODE or 8). Passed to
                        multi_node create-rayjob.
  --rayjob-extra-env K=V
                        Extra env entries to inject into the multi-node RayJob
                        (repeatable). Agent maps each line of the user
                        prompt's `env:` block into one --rayjob-extra-env K=V;
                        the CLI does not own any default. Skip *_API_KEY /
                        *_BASE_URL (auto-injected by _credential_fanout) and
                        RAY_JOB_ENTRYPOINT (reserved by workload_spec). Only
                        takes effect when --nodes>=2 and this run actually
                        creates the RayJob; idempotent reuse of an existing
                        rayjob_id keeps the env set at original create time.
  --tp TP               Tensor parallel size. Resolution: --tp > $TP env > 1.
                        Symmetric with --ep — historically TP only flowed in
                        via $TP env (read by _workload_envs); the CLI flag was
                        added so the agent can pass `--tp N` directly from the
                        prompt's Environment block instead of having to
                        `export TP=N` first. Either path still works.
  --conc CONC           Magpie client concurrency cap (max in-flight
                        requests). Resolution: --conc > $CONC env > 8.
                        Symmetric with --tp; agent can pass `--conc N`
                        directly from the prompt.
  --ep EP               Expert-parallel size for MoE inference. 1 (default)
                        keeps experts sharded by TP (legacy behaviour). >=2
                        enables true expert parallelism: sglang adds
                        `--expert-parallel-size N`, vllm adds `--enable-
                        expert-parallel`. Typical: EP=TP for DSr1/DSv3 on
                        multi-node. Resolution: --ep > $EP env > 1. EP > TP is
                        rejected at server-restart time.
  --pd-mode {colocated,disaggregated}
                        Prefill-Decode disaggregation mode. ALWAYS defaults to
                        `colocated` regardless of any inherited $PD_MODE env,
                        so PD only turns on when the agent explicitly passes
                        `--pd-mode disaggregated` (driven by the prompt's
                        Environment block having a PD_MODE=disaggregated
                        line). Stale env from a previous restart cannot
                        accidentally re-enable PD.
  --pd-prefill-nodes PD_PREFILL_NODES
                        Number of prefill nodes (disaggregated only);
                        pn+dn=nodes
  --pd-decode-nodes PD_DECODE_NODES
                        Number of decode nodes (disaggregated only)
  --pd-prefill-tp PD_PREFILL_TP
                        TP for prefill group (disaggregated only); default =
                        --tp
  --pd-decode-tp PD_DECODE_TP
                        TP for decode group (disaggregated only); default =
                        --tp
  --pd-transfer-backend PD_TRANSFER_BACKEND
                        sglang: mooncake|nixl ; vllm: NixlConnector|...; empty
                        = default
  --pd-ib-device PD_IB_DEVICE
                        comma-separated IB/RoCE device list (e.g.
                        mlx5_0,mlx5_1). Empty = use $NCCL_IB_HCA from RayJob
                        pod env at server-launch time.
  --skip-variants SKIP_VARIANTS
                        Comma/whitespace-separated list of variant names or
                        fnmatch globs to drop from the backends/params grids
                        before launch. Examples: `attn_aiter` (exact),
                        `attn_aiter,sched_dfs` (two exacts),
                        `attn_*,vllm_aiter_*` (globs). Resolution: --skip-
                        variants > $SKIP_VARIANTS > empty. Exported back into
                        $SKIP_VARIANTS so all executors and the multi-node
                        orchestrator subprocess see the same value. Dropped
                        variants surface in state.json under each action's
                        `dropped_variants` field tagged `source=user_skip`.
  --max-hours MAX_HOURS
                        Wall-clock budget in hours (default 2.0)
  --closing-grace-sec CLOSING_GRACE_SEC
                        Extra seconds after the wall-clock deadline for
                        Coordinator to flush a deterministic report task (no
                        LLM). Default: min(120, max_hours * 60 * 0.02). Pass 0
                        to disable closing phase.
  --isl ISL             Input sequence length (default $ISL or 256)
  --osl OSL             Output sequence length (default $OSL or 256)
  --precision PRECISION
                        Model precision (default $PRECISION or bf16)
  --framework-version FRAMEWORK_VERSION
                        Framework version slug for the recipe-snapshot
                        canonical id (scopes recipes to a specific framework
                        release — sglang 0.4.5 and sglang 0.5.x have different
                        scheduler defaults so they deserve separate KB rows).
                        When omitted, auto-detected via importing the
                        framework's top-level package and reading
                        ``__version__`` (sglang/vllm/atom supported); auto-
                        detect failure degrades to 'unknown_version'. Override
                        with --framework-version=0.4.5 to pin a specific tag
                        for the run.
  --continue-kernel-after-gemm, --no-continue-kernel-after-gemm
                        After FP8 GEMM tuning succeeds, continue into source-
                        level kernel_opt. Use --no-continue-kernel-after-gemm
                        for a GEMM-only validation run.
  --target-gain TARGET_GAIN
                        Stop when cumulative_gain >= N% over baseline
  --target-tput TARGET_TPUT
                        Stop when current best tok/s/GPU >= N
  --target-baseline-dir TARGET_BASELINE_DIR
                        Stop when current best matches the baseline in DIR
  --resume              Resume an existing session. Without --resume-from,
                        auto-picks the latest per-session subdir under
                        $USER_DATA_PATH/<model>/<UTC ts>/ (N17 layout) or
                        falls back to $USER_DATA_PATH (legacy flat layout).
                        USER_DATA_PATH MUST stay at workspace level
                        (/wekafs/.../sessions/, not the per-session subdir) so
                        runtime/ resolution works. Skips the SharedState seed
                        and lets the Coordinator replay the prior event log +
                        state.json.
  --resume-from RESUME_FROM
                        Explicit per-session subdir to resume from. Use when
                        multiple per-launch ts dirs exist under the same model
                        and the latest is not what you want. Must be an
                        absolute path under $USER_DATA_PATH (workspace_root).
                        Implies --resume.
  --force-resume        Allow ``--resume`` to push past a terminal
                        ``stop_reason='no_more_leverage'`` or
                        ``'target_reached'``. Without this flag the resume
                        aborts (Issue-G guard, per SKILL.md 'Run-time
                        signals': those terminals require an operator-side
                        workload / strategy change before resuming). No-op
                        outside ``--resume``.
  --model-class MODEL_CLASS
                        Categorical model-class key. It is the deterministic
                        key for several consumers: the atom explore seed grid
                        (action_executors/explore.py), the framework-agent gap
                        search token
                        (action_executors/_framework_gap_composer.py), the
                        recipe key, and the orchestration prompt label.
                        Recognised values (case-insensitive, with -/+/space
                        tolerated): dense / moe_mla / moe_swa / moe_mla_nsa.
                        When unset, Coordinator boot infers and persists it
                        from config.json (num_experts / architectures) or
                        model-path family keywords, replacing the deleted
                        ``classify`` action's lightweight state-write role.
                        For richer *advisory* model context (attention
                        variant, KV/token, experts, MTP, ...) the SKILL
                        launcher writes $USER_DATA_PATH/model_arch.json, which
                        is injected into prompts but drives no gating.
  --target-summary TARGET_SUMMARY
                        Free-text goal summary surfaced in prompts
  --compare-against-gpu COMPARE_AGAINST_GPU
                        Reference GPU hardware key for external baseline
                        comparison (e.g. b300 / mi355x / h200).
                        target_analysis ALWAYS runs as TODO 0 and always
                        writes
                        $SESSION_DIR/target_analysis/target_baseline.json + a
                        short MD report. When this flag is set, the JSON
                        carries the matching InferenceX
                        (https://inferencex.semianalysis.com) reference data
                        point; when unset, the JSON carries a structured
                        reason='no_target_gpu_configured' marker so the report
                        still has a deterministic 'External baseline' section.
                        The data is REPORT-ONLY: it does not influence
                        Objective, scoring, or any agent prompt. Other
                        dimensions (model / framework / precision / ISL / OSL)
                        are derived from --model and the standard FRAMEWORK /
                        PRECISION / ISL / OSL env vars.
  --max-ticks MAX_TICKS
                        Hard tick cap (None = unlimited; mostly for tests)
  --tick-interval-sec TICK_INTERVAL_SEC
                        Sleep between ticks (0 = no sleep)
  --claude-model CLAUDE_MODEL
  --codex-model CODEX_MODEL
  --no-kernel           Disable the Kernel agent entirely. The run will only
                        do baseline + params + backends + sweep (pure
                        parameter search). Useful when GEAK/OOB/GPU compile
                        env is unavailable or you just want the quick-win
                        parameter path. Default: kernel enabled.
  --no-explore          Skip the EXPLORE phase entirely. PRELUDE (and
                        FRAMEWORK_PR, if enabled) route straight to KERNEL —
                        or to SWEEP when --no-kernel is also set. Useful for a
                        baseline -> kernel-only run, or to validate the
                        current recipe via SWEEP without a serving-param
                        search. Default: explore enabled.
  --launch-info-file LAUNCH_INFO_FILE
                        Write a JSON file with the launched session's pid,
                        session_dir, session_id, run_log, manifest path,
                        gpu_type, framework and model. Launcher scripts can
                        ``jq -r .pid`` / ``jq -r .session_dir`` instead of
                        grepping stdout or pgrep'ing. Always emitted alongside
                        the ``HYPERLOOM_LAUNCH <key=value> ...`` single-line
                        sentinel that is printed to stdout for stream-based
                        parsers.
  --framework-pr-discover-timeout-sec FRAMEWORK_PR_DISCOVER_TIMEOUT_SEC
                        Override the per-call timeout for ``fa phase-
                        discover``. 0 (the default) uses
                        framework_agent_client.DEFAULT_FA_PHASE_TIMEOUT_SEC
                        (180s). The Coordinator retries discover up to
                        DISCOVER_FAILURE_RETRY_LIMIT (3) consecutive failures
                        before marking FRAMEWORK_PR done.
  --no-framework        Skip the FRAMEWORK_PR phase (PRELUDE → EXPLORE
                        directly). The phase pre-scans upstream sglang/vllm
                        PRs via framework-agent and lands KEPT patches before
                        EXPLORE starts. Disable when the framework-agent
                        toolchain is unavailable or you want a faster cold
                        start. Also read from
                        $INFERENCE_OPTIMIZER_NO_FRAMEWORK=1. Default:
                        framework phase enabled.
  --kernel-codex        Use Codex backend for Kernel agent (default — faster).
                        Pass --kernel-claude to switch.
  --kernel-claude       Use Claude backend for Kernel agent
  --critic-mock         Force the always-approve mock Critic (offline / smoke
                        tests).
  --critic-agent        Force the critic-agent runtime backend (KB + session
                        memory + review_constraints). Requires
                        CRITIC_AGENT_ROOT or a sibling $REPO_ROOT/critic-
                        agent/ directory.
  --robustness-mock     Force the heartbeat-only mock Robustness backend.
  --robustness-agent    Force the robustness-agent runtime backend (subprocess
                        + JSON, mirrors critic-agent transport). Requires
                        ROBUSTNESS_AGENT_ROOT or a sibling
                        $REPO_ROOT/robustness-agent/ directory.
  --robustness-server-url ROBUSTNESS_SERVER_URL
                        Override the robustness-server base URL forwarded into
                        request.options. Honoured only when --robustness-agent
                        is selected.
  --robustness-llm-rca  Forward llm_rca_enabled=true into request.options. The
                        agent still falls back to NoopRcaEngine when LLM
                        credentials aren't set in the runtime env.
  --no-robustness-llm-rca
                        Forward llm_rca_enabled=false into request.options.
  --robustness-workload-uid ROBUSTNESS_WORKLOAD_UID
                        Forward workload_uid into request.options. The
                        robustness-server resolves it to every pod (head +
                        workers) backing the RayJob via the
                        cluster/workloads/{uid}/hierarchy endpoint. Falls back
                        to $CLAW_WORKLOAD_UID / $WORKLOAD_UID / $RAY_JOB_ID
                        when unset.
  --robustness-disable-local-probe
                        Force disable_local_probe=true. The robustness-agent
                        silences its LocalProbe fallback so per-pod sandbox
                        checks (ps, rocm-smi, local HTTP) cannot emit false-
                        positive symptoms.
  --no-robustness-disable-local-probe
                        Force disable_local_probe=false (keep the LocalProbe
                        fallback even in multi-node mode).
  --robustness-enable-cluster-pod-metrics
                        Force enable_cluster_pod_metrics=true so the
                        robustness-agent fans out per-pod metrics through
                        robustness-server and feeds the local_health rules
                        with cluster-decoded GPU snapshots.
  --no-robustness-enable-cluster-pod-metrics
                        Force enable_cluster_pod_metrics=false.
  --robustness-pod-metrics-categories ROBUSTNESS_POD_METRICS_CATEGORIES
                        Comma-separated metric categories forwarded into
                        pod_metrics_categories (e.g. 'gpu,memory'). Default
                        'gpu' is applied by the runtime when this flag is
                        omitted.
  --orch-prompt ORCH_PROMPT
                        Override Orchestration system prompt (file path or
                        inline)
  --critic-prompt CRITIC_PROMPT
                        Override Critic system prompt
  --kernel-prompt KERNEL_PROMPT
                        Override Kernel system prompt
  --cortex-kb-url CORTEX_KB_URL
                        Remote recipe-snapshot KB URL (read-only) for this
                        run; also settable via $CORTEX_KB_URL. Leave it UNSET
                        to run fully local — there is no default endpoint, so
                        the optimizer never connects to a remote KB unless you
                        pass this explicitly. Writes always go to --local-kb-
                        root regardless; an explicitly-configured but
                        unreachable URL degrades the dispatcher to local-only
                        transparently (no need to also pass --degraded-kb).
  --local-kb-root LOCAL_KB_ROOT
                        Filesystem root for the local recipe-snapshot KB
                        store. All writes (put_recipe / append_attempt /
                        delete_recipe) go here regardless of --cortex-kb-url.
                        Defaults to $HYPERLOOM_LOCAL_KB_ROOT, then
                        $USER_DATA_PATH/kb, then /workspace/hyperloom/kb.
                        Layout is a 5-level directory tree keyed by
                        canonical_id components (model -> hardware ->
                        framework -> framework_version -> precision); each
                        leaf holds recipe.json + history/ + attempts.ndjson +
                        .lock. See
                        inference_optimizer/recipe_kb/local_store.py for the
                        on-disk contract.
  --degraded-kb         Skip the Cortex KB integration entirely (T0/T2/T3/T4
                        become no-ops). Also short-circuits the IR-3 KB probe.
                        IR-3 sets this automatically when kb-service is
                        unreachable (soft degrade); manifest records the
                        reason as ``explicit_flag`` vs ``ir3_auto``.
  --cortex-strict-fingerprint
                        When set, T0 refuses warm_start_recipe rows whose
                        stack_fingerprint does not match the current pod
                        (recorded in manifest.json). Default: lenient (M1
                        records the flag in manifest only; consumed by M5
                        specialist assembly).
  --no-warm-replay      Disable the PRELUDE auto-replay of KB warm-start
                        ``best_config``. The warm_start_recipe is still
                        rendered into the specialist prompt as priors, but the
                        Coordinator will NOT auto-run the historical
                        best_config. Use this for cold debugging / ablation
                        runs.
  --warm-replay-min-confidence WARM_REPLAY_MIN_CONFIDENCE
                        Minimum ``warm_start_recipe.confidence`` required to
                        trigger the auto-replay. Default 0.7 means an
                        ``exact`` 5-tuple hit (conf 1.0) and a server-returned
                        ``relative`` match (conf 0.7) both fire, while a
                        ``miss`` (conf 0.0) does not. Raise it above 0.7 to
                        require an exact hit before spending a verify on the
                        warm config.
  --warm-replay-min-reproduce-pct WARM_REPLAY_MIN_REPRODUCE_PCT
                        Minimum fraction of the recipe's recorded gain we need
                        to reproduce to count as ``status=reproduced`` and
                        push the warm config onto the optimization stack.
                        Default 0.8 — a recipe claiming +25% counts if we
                        measure +20% or more. Below the threshold we record
                        ``status=drift`` and continue with the regular EXPLORE
                        flow without inheriting the warm config.
  --no-kb-flusher       Skip spawning the Cortex KB flusher daemon. The NDJSON
                        pending queue still accumulates but only drains on the
                        next session start (or via manual ``python -m
                        inference_optimizer.scripts.cortex_kb_flusher`` run).
                        Implied by --degraded-kb.
  --kb-flusher-interval-sec KB_FLUSHER_INTERVAL_SEC
                        Forwarded to the flusher daemon as --interval-sec
                        (default 5.0).
  --kb-flusher-batch-size KB_FLUSHER_BATCH_SIZE
                        Forwarded to the flusher daemon as --batch-size
                        (default 50).
  --pr-monitor-url PR_MONITOR_URL
                        Override PR Monitor REST URL for this run. Default:
                        http://primus-cortex-pr-api.primus-
                        cortex.svc.cluster.local/v1 (env: PR_MONITOR_URL).
                        Pair with --pr-monitor-mcp-url when port-forwarding
                        for local debug.
  --pr-monitor-mcp-url PR_MONITOR_MCP_URL
                        Override PR Monitor MCP URL handed to specialist LLM
                        backends. Default mirrors --pr-monitor-url with /mcp/
                        suffix; the trailing slash is mandatory.
  --degraded-pr         Disable the PR Monitor integration entirely.
                        pr_feed_warm returns empty; the specialist tool
                        whitelist drops mcp__pr_monitor__* tools. Short-
                        circuits the IR-3 PR Monitor probe; IR-3 sets this
                        automatically when PR Monitor is unreachable (soft
                        degrade).
  --pr-feed-window-days PR_FEED_WINDOW_DAYS
                        Look-back window for the PR feed warmup (days).
                        Default: 30.
  --research-lane-capacity RESEARCH_LANE_CAPACITY
                        Max concurrent LLM specialist sub-agents on the
                        research_lane. 0 disables specialist dispatch entirely
                        (degrades to LLM-direct grid); 4 is the default. The
                        upper bound scales with the visible GPU count (2 x
                        GPU); values above it are silently clamped down.
                        Locked at session start.
  --gpu-specialist-capacity GPU_SPECIALIST_CAPACITY
                        Number of GPUs available to specialists that request
                        needs_gpu=true. 0 disables GPU specialists (default).
                        Set INFERENCE_OPTIMIZER_GPU_SPECIALIST_DEVICES to a
                        comma-separated GPU id pool when the specialist pool
                        should not use device ids 0..N-1. Locked at session
                        start.
  --proposal-scorer-models PROPOSAL_SCORER_MODELS
                        Comma-separated gateway model slugs that independently
                        score each specialist proposal_set (advisory only;
                        never gates; rater identities are anonymized in the
                        orchestration prompt). Default 'claude-
                        opus-4-8,gpt-5.5,dvue-
                        aoai-005-Kimi-K2.6,gemini/gemini-3.1-pro-preview'. Add
                        a model by appending its slug. Empty list disables
                        scoring.
  --no-proposal-scoring
                        Disable the advisory specialist-proposal scorer
                        entirely.
  --specialist-model SPECIALIST_MODEL
                        Claude model used for specialist sub-agents (defaults
                        to the orchestration --claude-model). KB_design §3.5
                        §6.
  --dynamic-action-model DYNAMIC_ACTION_MODEL
                        Claude model used for dynamic_action sub-agents
                        (defaults to --claude-model).
  --dynamic-action-turn-cap DYNAMIC_ACTION_TURN_CAP
                        Hard cap on ReAct turns per dynamic_action dispatch
                        (default 12). Per
                        dynamic_action_runner.DEFAULT_TURN_CAP.
  --dynamic-action-wall-clock-sec DYNAMIC_ACTION_WALL_CLOCK_SEC
                        Wall-clock budget per dynamic_action dispatch (default
                        900s = 15 min). Per
                        dynamic_action_runner.DEFAULT_WALL_CLOCK_BUDGET_SEC.
  --specialist-max-turns SPECIALIST_MAX_TURNS
                        Hard cap on LLM turns per specialist task (KB_design
                        §3.5 §6). On exhaustion the runner synthesises an
                        empty specialist_done (Inv-5.3).
  --specialist-per-turn-max-seconds SPECIALIST_PER_TURN_MAX_SECONDS
                        Wall-clock cap per specialist turn (default 600s).
                        Used by the robustness stale-scan to detect stuck
                        specialists .
  --specialist-dispatch-mode {subprocess,inprocess}
                        Specialist execution shape. 'subprocess' (default)
                        spawns a fresh `claude` CLI per task inside a per-task
                        git worktree for isolation (PR-A2). 'inprocess' keeps
                        the legacy M5 path (claude-agent-sdk in the
                        orchestrator process) for tests / environments without
                        the claude binary.
  --specialist-mcp-config SPECIALIST_MCP_CONFIG
                        Optional path to an MCP config JSON forwarded to the
                        subprocess claude (`--mcp-config`). Used to wire kb /
                        pr MCP servers into specialists. Default: None.
  --allow-empty-kernel-shape
                        Escape hatch (default off): allow kernel optimization
                        to dispatch a candidate with no trace-anchored shape.
                        Normally a shapeless candidate is rejected with a
                        structured error so the run returns to
                        ``trace_analyze`` instead of burning a GEAK / OOB
                        budget on an unanchored kernel. Env:
                        HYPERLOOM_ALLOW_EMPTY_KERNEL_SHAPE=1.
  --enable-roofline, --no-enable-roofline
                        Select which analysis action the Coordinator enqueues
                        at PRELUDE bootstrap and on every +10% watermark
                        crossing. Default on: ``roofline`` (composite profile
                        + trace_analyze + analysis.md). Pass ``--no-enable-
                        roofline`` to use plain ``profile`` instead (lighter —
                        captures the trace only, skips trace_analyze).
                        Behaviour is otherwise identical (same idempotency
                        keys, same pending-task dispatch gate, same watermark
                        anchor update). Env:
                        INFERENCE_OPTIMIZER_ENABLE_ROOFLINE=0.
  --research-scout, --no-research-scout
                        Auto-dispatch a read-only research scout at PRELUDE
                        (and every --research-scout-interval EXPLORE rounds)
                        that collects proven priors — reference launch
                        scripts, model config.json architecture features, and
                        cross-framework / NVIDIA research — into
                        ``research_hints.md`` and seeds high-priority gaps.
                        Default on; pass ``--no-research-scout`` to disable
                        the whole feature. Env:
                        INFERENCE_OPTIMIZER_RESEARCH_SCOUT=0.
  --research-scout-interval RESEARCH_SCOUT_INTERVAL
                        Re-dispatch the research scout every N EXPLORE rounds
                        with the current bottleneck context (append-only).
                        Default 3. Ignored when ``--no-research-scout`` is
                        set.
  --recipe-sediment, --no-recipe-sediment
                        Sediment KEEP/REVERT provenance into the persistent
                        recipe: KEEP optimizations traceable to a research
                        hint carry their source + measured gain into
                        ``what_worked``; REVERTs land in ``what_failed`` so
                        the next warm-start avoids re-testing them. Default
                        on; pass ``--no-recipe-sediment`` to keep the recipe
                        purely ephemeral. Env:
                        INFERENCE_OPTIMIZER_RECIPE_SEDIMENT=0.
  --target-advisory, --no-target-advisory
                        Inject an advisory 'External target gap' block
                        (throughput / TPOT / interactivity gap vs the LLM-
                        authored competitor target) into the orchestration and
                        specialist prompts; when the TPOT ratio dominates it
                        nudges toward latency-reducing directions. Advisory
                        only — never gates Objective or scoring. Default on;
                        pass ``--no-target-advisory`` to disable. Env:
                        INFERENCE_OPTIMIZER_TARGET_ADVISORY=0.
  --depth-gate, --no-depth-gate
                        Deterministic exploration-depth guard. When the
                        session steward recommends stop / advance-to-kernel
                        but the run has not explored deeply enough (too few
                        scout runs, no code patch, too few PRs/diffs/NVIDIA
                        refs), the verdict is rewritten to continue_explore
                        with a deepening hint — as many times as needed,
                        bounded only by the IR-6 budget gate. Default on;
                        ``--no-depth-gate`` restores the legacy single-
                        continuation steward behaviour. Env:
                        INFERENCE_OPTIMIZER_DEPTH_GATE=0.
  --depth-gate-thresholds DEPTH_GATE_THRESHOLDS
                        Override depth-gate minimums as comma-separated
                        key=value pairs. Keys: scout_runs_min,
                        prs_fetched_min, pr_diffs_read_min, nvidia_refs_min,
                        code_patches_min, reverts_to_evaluate. Defaults:
                        2,5,3,2,1 evaluated after reverts_to_evaluate=3.
                        Example: ``--depth-gate-thresholds
                        scout_runs_min=1,code_patches_min=2``.
  --enable-conc-sweep, --no-enable-conc-sweep
                        Run a post-optimization concurrency sweep (baseline vs
                        current_best across CONC) and write
                        reports/conc_sweep_summary.json + conc_sweep_raw.csv.
                        On by default; disable with --no-enable-conc-sweep or
                        INFERENCE_OPTIMIZER_ENABLE_CONC_SWEEP=0.
  --conc-sweep-concs CONC_SWEEP_CONCS
                        Comma-separated CONC ladder for --enable-conc-sweep.
                        Default 1,2,4,8,16,32,64,128.
  --conc-sweep-timeout-sec CONC_SWEEP_TIMEOUT_SEC
                        Per-variant timeout (seconds) for --enable-conc-sweep.
                        Default 1800 (~30 min). Per-variant cap is also
                        clamped by the remaining --conc-sweep-total-budget-
                        sec.
  --conc-sweep-total-budget-sec CONC_SWEEP_TOTAL_BUDGET_SEC
                        Total wall-clock budget (seconds) for the whole conc-
                        sweep action, independent of the per-variant Magpie
                        timeout. Once exhausted, remaining variants are
                        recorded as status=skipped /
                        error_class=budget_exhausted and the JSON envelope
                        carries budget_exhausted=true. Default 9000 (~2.5h);
                        set to 0 to disable. Also bounded above by the main
                        session wall-clock deadline since conc_sweep runs as a
                        SWEEP-phase action.
  --explore-overtime-kill-ratio EXPLORE_OVERTIME_KILL_RATIO
                        Per-variant explore overtime kill: each single-variant
                        Magpie run in the explore loop is reaped once its
                        wall-clock exceeds ``baseline_runtime_sec * RATIO``.
                        The variant is recorded with outcome=KILLED_OVERTIME +
                        runtime_sec + wall_clock_ratio_vs_baseline (no tput)
                        so the LLM can distinguish it from a hard timeout /
                        crash. Default 1.10 (kill at +10% over baseline wall-
                        clock). Pass 0 to disable. Env:
                        INFERENCE_OPTIMIZER_EXPLORE_OVERTIME_KILL_RATIO.
  --explore-variant-timeout-sec EXPLORE_VARIANT_TIMEOUT_SEC
                        Pin the per-variant hard timeout (seconds) inside the
                        EXPLORE phase. ``0`` (default) auto-derives from
                        ``baseline_runtime_sec * (--explore-overtime-kill-
                        ratio + --explore-variant-timeout-safety-margin)``
                        once baseline lands, with a 2400-14400 s range guard.
                        Set to a positive integer to pin (CI smoke runs /
                        debugging). Env:
                        INFERENCE_OPTIMIZER_EXPLORE_VARIANT_TIMEOUT_SEC.
  --explore-variant-timeout-safety-margin EXPLORE_VARIANT_TIMEOUT_SAFETY_MARGIN
                        Headroom (as a fraction of baseline_runtime_sec) added
                        on top of --explore-overtime-kill-ratio when the
                        EXPLORE hard cap is auto-derived. Default 0.5 (≈ 50%
                        of baseline as buffer for variant cold starts:
                        torch.compile AOTI compile, fresh aiter shapes, spec-
                        decoding draft load). Bump for workloads with heavy
                        compile cost; lower to tighten the backstop. No effect
                        when --explore-variant-timeout-sec is set to a
                        positive value. Env: INFERENCE_OPTIMIZER_EXPLORE_VARIA
                        NT_TIMEOUT_SAFETY_MARGIN.
  --explore-roofline-hard-gate
                        (Opt-in, off by default.) Drop EXPLORE variants whose
                        flags target only roofline directions that the latest
                        snapshot shows saturated above 80%. Saves variant
                        slots on slow workloads where (e.g.) host-overhead
                        reducers cannot help a memory-bound model. The
                        existing soft ``--roofline-saturation-advisory``
                        prompt hint is unchanged either way. Env:
                        INFERENCE_OPTIMIZER_EXPLORE_ROOFLINE_HARD_GATE=1.
  --legacy-action-scores {drop,warn}
                        Resume-mode handling of the legacy scoreboard
                        (``action_scores`` and friends). 'drop' (default)
                        silently discards. 'warn' logs a WARNING + adds a
                        breakdown.warnings entry. KB_design §3.9 §7.
  --migration-mode {strict,lenient}
                        Strictness of the legacy → v0.8 state.json migration.
                        'strict' (default) aborts on fact-layer field loss;
                        'lenient' logs WARNING and continues. KB_design §3.10
                        §5.3.
  --reset-state         Back up the existing ``state.json`` (if any) to
                        ``state.json.preReset.<unix_ts>`` and start the
                        session from a blank SharedState. Cortex KB is NOT
                        touched. KB_design §3.10 §5.3.
  --breakdown-include-transcripts {true,false}
                        Inline specialist transcript bodies into
                        ``specialist_runs`` (true) or reference them by path
                        only (false, default). KB_design §3.12 §7.
  --plateau-explore-keep-gain PLATEAU_EXPLORE_KEEP_GAIN
                        EXPLORE plateau: max cumulative KEEP-gain (%) across
                        the lookback window below which the AND condition
                        fires. Default 0.5.
  --plateau-explore-empty-streak PLATEAU_EXPLORE_EMPTY_STREAK
                        EXPLORE plateau: required count of *consecutive*
                        specialist rounds with empty proposal_set before the
                        AND condition fires. Default 3.
  --plateau-explore-lookback PLATEAU_EXPLORE_LOOKBACK
                        EXPLORE plateau: number of trailing rounds the gain
                        sum is computed over. Default 5.
  --plateau-kernel-revert-streak PLATEAU_KERNEL_REVERT_STREAK
                        KERNEL plateau: consecutive REVERT / NEEDS_REVIEW
                        integrate attempts to count as plateau (one half of
                        the OR). Default 3.
  --plateau-kernel-keep-gain PLATEAU_KERNEL_KEEP_GAIN
                        KERNEL plateau: max cumulative KEEP-gain (%) across
                        the lookback window below which the OR fires. Default
                        0.5.
  --plateau-kernel-lookback PLATEAU_KERNEL_LOOKBACK
                        KERNEL plateau: number of trailing integrate attempts
                        the gain sum is computed over. Default 5.
  --explore-force-exit-hours-remaining EXPLORE_FORCE_EXIT_HOURS_REMAINING
                        EXPLORE force-exit: total wall-clock remaining (hours)
                        below which EXPLORE exits immediately to the next
                        phase, regardless of plateau / steward. Default 3.0
                        (IR-6).
  --explore-force-exit-budget-pct EXPLORE_FORCE_EXIT_BUDGET_PCT
                        EXPLORE force-exit: phase-budget remaining fraction
                        (0..1) below which EXPLORE exits immediately. Default
                        0.20 (IR-6).
  --steward-disabled    Disable session_steward_specialist; plateau directly
                        exits EXPLORE without the steward gate (IR-7).
  --steward-continuation-cap STEWARD_CONTINUATION_CAP
                        Max times the steward may return 'continue_explore' in
                        this session (default 1). Beyond the cap,
                        continue_explore is coerced to advance_to_kernel.
  --max-minutes-prelude-pct PHASE_BUDGET_PRELUDE_PCT
                        Wall-clock budget cap for PRELUDE as a fraction of
                        --max-hours. Default: 0.05.
  --max-minutes-explore-pct PHASE_BUDGET_EXPLORE_PCT
                        Wall-clock budget cap for EXPLORE. Default: 0.40.
  --max-minutes-kernel-pct PHASE_BUDGET_KERNEL_PCT
                        Wall-clock budget cap for KERNEL. Default: 0.35.
  --max-minutes-sweep-pct PHASE_BUDGET_SWEEP_PCT
                        Wall-clock budget cap for SWEEP. Default: 0.18.
  --max-minutes-close-pct PHASE_BUDGET_CLOSE_PCT
                        Wall-clock budget cap for CLOSE. Default: 0.02.
  --strict-phase        (v0.8 M2 default) Enforce PolicyGate R1
                        phase_incompatible. Action proposals outside the
                        current phase's allowlist return policy_denied so the
                        LLM self-corrects.
  --no-strict-phase     Disable R1 enforcement (warn-only). Useful for back-
                        compat smoke tests; production should stay strict.
