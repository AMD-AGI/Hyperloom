API Reference
=============

The pages below are generated automatically from the project's source
docstrings. Each top-level package is expanded recursively, so every
sub-module, class, and function with a docstring is documented.

The reference covers the importable Python packages. ``ci``,
``kernel-agent``, and ``slides`` are script collections that are not laid out
as importable packages, so they are documented via their in-code docstrings
rather than these autosummary pages.

.. autosummary::
   :toctree: generated
   :recursive:

   inference_optimizer
   robustness_agent
   framework_agent
