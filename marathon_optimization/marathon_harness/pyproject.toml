[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "marathon-harness"
version = "0.1.0"
description = "Marathon Inference Optimization Harness — 24h autonomous GPU optimizer"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
marathon = "marathon_harness.marathon:main"

[tool.setuptools.packages.find]
where = ["."]
