[pytest]
# PR1 skeleton: tests/ directory lands in PR2 along with the first real
# test modules. Until then keep pytest happy with no testpaths so that
# `pytest framework-agent/` returns 0 items collected (not an error).
pythonpath = src
filterwarnings =
    error::SyntaxWarning
    ignore::DeprecationWarning
