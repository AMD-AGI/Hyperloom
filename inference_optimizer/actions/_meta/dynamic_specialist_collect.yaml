name: dynamic_specialist_collect
family: shallow
cost_minutes_p50: 0.5
cost_minutes_p75: 1.0
expected_gain_pct: [0.0, 0.0]
accuracy_risk: 0.0
crash_risk: 0.0
prerequisites:
  - baseline
requires_lanes: []
allowed_tools:
  - emit_intent
side_effects:
  - reads_results
preferred_backend: claude
preferred_model: claude-opus-4-7
max_turns: 4
lease_ttl_sec: 300
description: "Collect the full completion report for a finished dynamic specialist by agent id."
pipeline_phase: explore
typical_runtime_min: 0.5
verdict_class: exploration
applicable_when:
  - "phase == 'EXPLORE'"
params_schema:
  agent_id:
    type: string
    description: |
      Required. Agent id returned in ``dynamic_specialist_dispatched`` or
      ``dynamic_specialist_completed`` observations.
