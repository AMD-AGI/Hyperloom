name: compiler_tuning
family: long
cost_minutes_p50: 45.0
cost_minutes_p75: 90.0
expected_gain_pct: [2.0, 10.0]
accuracy_risk: 0.05
crash_risk: 0.10
prerequisites:
  - profile
requires_lanes:
  - server_lifecycle
  - workspace_mutation
  - benchmark_lane
allowed_tools:
  - emit_intent
  - Read
  - Bash
  - Edit
side_effects:
  - workspace_write
  - server_restart
preferred_backend: claude
preferred_model: claude-opus-4-7
max_turns: 30
lease_ttl_sec: 5400
description: "Tune torch.compile / Inductor knobs (cache, max-autotune, cudagraph) when the active server runs with compile enabled."
pipeline_phase: support
typical_runtime_min: 45.0
applicable_when:
  - "torch_compile_enabled"
