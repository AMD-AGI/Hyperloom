name: sweep
family: shallow
cost_minutes_p50: 15.0
cost_minutes_p75: 30.0
expected_gain_pct: [3.0, 10.0]
accuracy_risk: 0.0
crash_risk: 0.05
prerequisites:
  - params
requires_lanes:
  - server_lifecycle
  - benchmark_lane
allowed_tools:
  - emit_intent
  - Read
  - Bash
side_effects:
  - launches_server
  - writes_results
preferred_backend: claude
preferred_model: claude-opus-4-7
max_turns: 25
lease_ttl_sec: 2400
description: "Workload-frontier sweep over (CONC, ISL, OSL) on top of the current best to validate gains beyond the smoke workload."
pipeline_phase: explore
typical_runtime_min: 15.0
applicable_when:
  - "baseline_tput > 0"
