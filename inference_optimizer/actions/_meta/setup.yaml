name: setup
family: prep
cost_minutes_p50: 2.0
cost_minutes_p75: 5.0
expected_gain_pct: [0.0, 0.0]
accuracy_risk: 0.0
crash_risk: 0.0
prerequisites: []
requires_lanes: []
allowed_tools:
  - emit_intent
  - Read
  - Bash
side_effects:
  - reads_filesystem
  - writes_state
preferred_backend: claude
preferred_model: claude-opus-4-7
max_turns: 8
lease_ttl_sec: 300
description: "Initialise session_id and persist run-level config (model, framework, gpu_type, paths)."
pipeline_phase: prep
typical_runtime_min: 2.0
applicable_when:
  - "session_id == ''"
