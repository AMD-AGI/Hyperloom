name: classify
family: prep
cost_minutes_p50: 1.0
cost_minutes_p75: 2.0
expected_gain_pct: [0.0, 0.0]
accuracy_risk: 0.0
crash_risk: 0.0
prerequisites:
  - setup
requires_lanes: []
allowed_tools:
  - emit_intent
  - Read
side_effects:
  - reads_model_files
  - writes_state
preferred_backend: claude
preferred_model: claude-opus-4-7
max_turns: 8
lease_ttl_sec: 300
description: "Classify the model (dense / moe_mla / moe_swa / ...) so downstream actions can prefer the right priors."
pipeline_phase: prep
typical_runtime_min: 1.0
applicable_when:
  - "model_class == ''"
