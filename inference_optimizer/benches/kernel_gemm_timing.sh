#!/usr/bin/env bash
# kernel_gemm_timing.sh — placeholder body wired through _placeholder.sh until the
# real probe lands. See dynamic_action_tools.BENCH_REGISTRY.
exec env BENCH_ID="kernel_gemm_timing" bash "$(dirname "$0")/_placeholder.sh" "$@"
